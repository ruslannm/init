# For example, new user's name is 'test' with non-empty password.
# for connection in GUI session you need 'Switch User' in top-right corner and login as 'test'
# for connection via ssh you need command ssh test@IP-address
